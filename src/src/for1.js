
var arreglo = new array(10) // campos que por defecto tiene el array 

for (let i = 0; i < arreglo.length; i++){ // llega hasta 9 puesto que el array por defecto empieza de 0
    arreglo[i] = i

}
console.log(arreglo)